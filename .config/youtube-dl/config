# No 4k video, no 60 FPS
-f "bestvideo[height<=1080]+bestaudio"

# Save file in ~/ytdl/Uploader/Title.extension
-o "~/video/ytdl/%(uploader)s_%(title)s.%(ext)s"

# Get subtitles as well
--all-subs
--embed-subs
