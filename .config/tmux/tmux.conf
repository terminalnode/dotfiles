# TPM install
run-shell "~/.config/tmux/install_tpm.sh"
set -g @plugin 'tmux-plugins/tpm'

# Change prefix to C-a, bind <prefix>r to reloading config
unbind C-b; unbind r
set -g prefix C-a
bind r source-file "~/.config/tmux/tmux.conf"

# Vim-like navigation, better colors, mouse support, top bar
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on
set-option -g status-position top

# Start windows and pane at 1 instead of 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Open panes in current directory, and rebind splitters to mimic vim
unbind '%'; unbind '"'
bind 's' split-window -v -c "#{pane_current_path}"
bind 'v' split-window -h -c "#{pane_current_path}"

# Vim-Tmux integration
set -g @plugin 'christoomey/vim-tmux-navigator'

# Theme
set -g @plugin 'catppuccin/tmux'
set -g @catppuccin_flavor 'latte'
set -g @catppuccin_window_status_style 'basic'
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ogq @catppuccin_window_text " #F#W"
set -ogq @catppuccin_window_current_text " #F#W"
run ~/.config/tmux/plugins/tmux/catppuccin.tmux

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
